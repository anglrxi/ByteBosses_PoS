# ByteBoss POS System
A Java POS system with automated inventory management.

## Setup Instructions
1. Import the `byteboss_pos.sql` file into your MySQL database (via phpMyAdmin).
2. Open the project in NetBeans.
3. Ensure the JAR in the `/lib` folder is added to your project libraries.
4. Run `NewJFrame.java` to start the application.
